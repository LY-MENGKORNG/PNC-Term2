          # FROM THE INTERNET
# from tkinter import *
# # import ttk as ttk
# root = Tk()
# l =Label(root, text="Starting...")
# l.grid()
# l.bind('<Enter>', lambda a: l.configure(text='Moved mouse inside'))
# l.bind('<Leave>', lambda a: l.configure(text='Moved mouse outside'))
# l.bind('<Button-1>', lambda a: l.configure(text='Clicked left mouse button'))
# l.bind('<Double-1>', lambda a: l.configure(text='Double clicked'))
# # l.bind('<B3-Motion>', lambda a: l.configure(text='right button drag to %d,%d' % (e.x, e.y)))
# root.mainloop()